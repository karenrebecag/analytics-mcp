node_modules/
dist/
*.log
.DS_Store

# Credentials and org-specific config — NEVER commit. The open-source repo
# ships sites.example.json; the real mapping lives only in env / this file.
.env
.env.*
# ...except the template. It names every variable and holds no value, and an
# ignored file rots unseen: nothing diffs it, so nobody notices when it breaks.
!.env.example
sites.config.json

# Probe captures may contain live analytics data from a real tenant
scratch/

# Private working notes — never committed
PRIVATE-NOTES.md
