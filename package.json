{
  "name": "analytics-mcp",
  "version": "0.1.0",
  "description": "Unified web-analytics MCP server — GA4, Cloudflare Web Analytics, Vercel Analytics and Google Search Console behind one normalized tool surface. StreamableHTTP for remote clients (Claude web), stdio for local ones.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "analytics-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "lint": "eslint src/ tests/",
    "format": "prettier --write 'src/**/*.ts' 'tests/**/*.ts'",
    "format:check": "prettier --check 'src/**/*.ts' 'tests/**/*.ts'",
    "test": "vitest run",
    "gates": "pnpm build && vitest run --config vitest.gates.config.ts",
    "verify": "pnpm check && pnpm lint && pnpm format:check && pnpm test && pnpm gates"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
